# X683 Vendor Delta Index — 2026-08-20

Proven vendor/Transsion areas include the F2FS GC controller (`tran_gc_thread_func`, `tran_do_f2fs_gc`, `tran_has_enough_free_segment`, `is_f2fs_fragmentation`), Transsion battery/charger (`tran_battery_probe`, `tran_btscharger_probe` and charger policy), performance monitoring, display shutdown integration, and Android Binder block/buffer monitoring.

MTK scheduler/PPM/EEM/PBM/thermal/GPU/display/storage modifications are classified from symbols, source-path fingerprints and call/data evidence rather than names alone.

Module-only vendor symbols are inventory evidence until module binaries are supplied. Names alone never promote a vendor behavior to proven semantics.
